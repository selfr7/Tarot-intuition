# PRD · 今日塔罗（小红书小工具）

> 版本：v0.1（待对齐）
> 形态：小红书小工具（离线 H5 打包 zip），遵循 `minitool-zip-builder` 规范
> 范围：仅 22 张大阿卡纳，支持单牌 / 三牌阵，支持逆位开关

---

## 1. 背景与目标

为小红书小工具容器开发一款「今日塔罗」占卜工具：纯本地离线运行，引导用户完成
**抽牌 → 看牌 → 解读 → 记录感受 → 发布笔记** 的完整闭环。

- **目标用户**：对塔罗/每日指引感兴趣的年轻用户
- **核心价值**：仪式感 + 引导式解读（不恐吓、不宿命，强调自我觉察）
- **成功标准**：一次完整抽牌流程可顺利完成；抽完可一键发布带牌面图的笔记

---

## 2. 整体流程

```
首页（问候+牌盒互动）
   └─ 点击牌盒 → 牌阵选择
         ├─ 单牌指引
         └─ 三牌阵（过去/现在/未来）
         ├─ 牌阵说明指引（可展开查看）
         └─ 逆位开关（默认 关）
   └─ 洗牌引导页（动画 + 默念问题提示 + 误导性提问提示 + 3 秒倒计时）
   └─ 抽牌页（22 张展开的牌列，按牌阵牌数点选）
   └─ 牌背页（已抽中的牌背面朝上，提示点击翻开）
   └─ 牌面页（正/逆位 + 牌面图 + 观想引导语）
         └─ 「看牌意」按钮 → 牌意页（正/逆位解读文案）
         └─ 「记录感受」按钮 → 输入框弹层 → 保存（localStorage）
   └─ 保存今日抽牌为笔记（postNote，配图为抽中的牌面图）
```

三牌阵时第 4~6 步逐张进行：每翻一张看一张、记一张，三张完成后可发布笔记。
（待确认：见 §8 Q3）

---

## 3. 功能需求明细

### 3.1 首页

| 项 | 说明 |
| --- | --- |
| 问候语 | 按时段变化：清晨（5–11）/ 午后（11–17）/ 暮色（17–23）/ 深夜（23–5），例：「暮色降临，适合安静地听一听内心的声音」 |
| 背景氛围 | 提供的背景图（或 CSS 渐变+星光粒子降级方案） |
| 互动入口 | 牌盒元素，点击触发「开始抽牌」，带轻微动效（悬浮呼吸/点击缩放） |
| 历史记录 | 无（只保存不展示） |

### 3.2 牌阵选择

| 项 | 说明 |
| --- | --- |
| 单牌指引 | 抽 1 张，定位：今日能量 / 一个提醒 |
| 三牌阵 | 抽 3 张，定位：过去 / 现在 / 未来 |
| 牌阵说明 | 每个牌阵卡片下方或点「?」展开说明文字，帮助新手选择 |
| 逆位开关 | Toggle，默认关闭；开启后抽中的牌有概率为逆位（正逆概率 待确认，建议 70/30） |
| 返回 | 可返回首页 |

### 3.3 洗牌引导页

- 洗牌动画（CSS 卡牌翻动/交错动效），不可跳过但无需点击
- 文案引导：*「闭上眼睛，在心里默念你想问的问题…」*
- 提示条（误导性提问纠正，轮播或静态两条）：
  - *「尽量避免『是/否』类选择性问题，试试『我该如何看待…』『我在…中该注意什么』」*
  - *「问题越具体，牌面越有回应」*
- 3 秒倒计时（数字倒计时），结束后自动进入抽牌页

### 3.4 抽牌页

- 22 张牌以扇形/横向滚动一列展开，均为牌背
- 顶部提示按已选牌阵显示：*「请凭直觉点选 1 张」* / *「请依次点选 3 张」*
- 点击一张即抽中（三牌阵需点满 3 张，已选中的牌高亮标记）
- 抽满后自动进入牌背页

### 3.5 牌背页

- 展示刚抽中的牌（背面朝上，单牌居中；三牌阵按位次排列）
- 位置标签：单牌「今日指引」；三牌阵「过去 / 现在 / 未来」
- 提示：*「深呼吸，点击牌背翻开」*

### 3.6 牌面页

- 翻牌动画（3D flip）
- 显示：牌面图（正位正向展示，逆位旋转 180°）、牌名（中英）、正/逆位标记、位次标签
- 观想引导语（每张牌翻开后显示）：
  *「看着这张牌——你看到了什么画面？牌中的人物在做什么、感受着什么？」*
- 底部两个按钮：
  - **看牌意** → 进入牌意页
  - **记录感受** → 弹出输入层
- 三牌阵：翻完当前张进入下一张的翻牌流程（待确认 Q3）

### 3.7 牌意页

- 完整牌意文案：关键词 + 牌意描述 + 指引建议（依据韦特体系，正/逆位分开）
- 逆位牌显示逆位文案
- 底部仍保留「记录感受」入口与「下一张 / 发布笔记」按钮

### 3.8 记录感受

- 点击弹出页内 Modal 输入框（textarea），placeholder 引导：
  *「这张牌让你联想到了什么？直觉、画面、最近的事…」*
- 保存写入 localStorage（key 如 `tarot_diary`），**不展示历史列表**
- 可跳过不填，不阻塞发笔记流程
- 一条记录结构：`{ date, spread, cards: [{name, orientation}], note }`

### 3.9 发布笔记（保存今日抽牌为笔记）

- 触发按钮：牌意页/流程末尾「保存今日抽牌为笔记」
- 调用 `window.xhs.miniTool.postNote`：
  - `mediaInfo.image_resources`：**抽中的牌面图**（包内图片转为 base64 data:uri；三牌阵传 3 张，按位次排序）
  - `title`：如「今日塔罗 · 愚者」（≤20 字）
  - `content`：日期 + 牌阵 + 牌名/正逆位 + 牌意关键词 + 用户感受（≤1000 字）
  - `tags`：如「塔罗,今日塔罗,每日一抽」
- 需由用户点击触发；容器接管后续发布流程
- 感受为空时正文不含感受段落

---

## 4. 内容资产

| 资产 | 数量 | 说明 | 状态 |
| --- | --- | --- | --- |
| 牌面图 | 22 张 | 大阿卡纳，建议 600×1000 左右 webp/jpg，单张 ≤200KB | **待用户提供** |
| 首页背景图 | 1 张 | 可选，没有则 CSS 渐变+粒子 | 待用户提供 |
| 牌意文案 | 22 × 正/逆位 | 基于韦特体系提炼为短文案（关键词/描述/建议三段） | 用户有参考文档/skill，我来撰写成稿 |
| 牌背图/装饰 | 1+ | 牌背可 CSS 绘制或提供图 | 待定 |
| 观想引导语/提示文案 | 若干 | 我撰写，评审时过一遍 | 我出初稿 |

**包体预算**：≤10MB（上限），目标 ≤5MB；图片过大时压缩。

---

## 5. 技术约束（小红书小工具规范）

- 纯离线：无任何网络请求，所有资源打进 zip
- 脚本外置 `.js` + `<script src>`：禁内联 script、行内 onclick、`eval`、module
- 图片相对路径引用，禁外部 URL；发笔记用 base64 data:uri
- 存储：localStorage（按工具隔离，不保证永久）
- 无历史列表、无下载、无外链跳转
- 适配：viewport `viewport-fit=cover`，安全区用 `var(--safe-area-inset-*, env(...))`，触摸优先 pointer events
- 入口 `index.html` 必须在 zip 根目录；仅一个 HTML 页面，视图 JS 切换

---

## 6. 边界与异常

| 场景 | 处理 |
| --- | --- |
| postNote 失败 / 容器无 bridge | toast 提示「当前环境不支持发布」，流程不中断 |
| 感受输入为空点保存 | 轻提示，允许直接关闭 |
| 抽牌中途退出 | 不做断点续抽，重进重新开始 |
| 逆位开关切换 | 仅影响本次抽牌，不持久化（待确认 Q4） |
| localStorage 不可用 | try/catch 静默降级，感受仅本次会话内存保存 |

---

## 7. 视觉基调（待确认）

- 风格：神秘静谧 + 轻仪式感；深色底（深蓝紫/墨绿）+ 金色点缀
- 字体：系统字体栈（规范禁止外链字体；可打包 woff2 但建议省略）
- 动效：洗牌动画、翻牌 3D flip、星光粒子点缀，克制不喧闹

---

## 8. 待确认问题

| # | 问题 | 建议 |
| --- | --- | --- |
| Q1 | 逆位出现概率 | 建议 70% 正 / 30% 逆 |
| Q2 | 牌面图素材的路径与命名规则（如 `00-fool.jpg`） | 用户开抽前提供 |
| Q3 | 三牌阵是逐张翻牌逐张解读，还是 3 张全部翻开后统一展示解读 | 建议逐张（仪式感强、引导清晰） |
| Q4 | 逆位开关是否记住上次选择（localStorage） | 建议不记，每次默认关 |
| Q5 | 发布笔记时若抽了 3 张，配图传 3 张牌面图 | 是 |
| Q6 | 牌背样式：CSS 绘制还是提供图 | 建议 CSS 绘制（省体积、风格统一） |
